import React from 'react';
import styled from 'styled-components';

import {callWeather} from '../api';

const WeatherPage = () => {

    //callWeather();

    return (
        <>
        </>
    );
};

export default WeatherPage;